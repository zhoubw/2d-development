#include "main.hpp";
#include "Mirror.hpp"; //contains Player and Projectile

const int width = 1280;
const int height = 720;

Player p(width/2, height/2);
Mirror mirror(p);
Projectile prj("blueBullet.png", 13,13,1024/2,0,270,2);
